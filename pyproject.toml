[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "winstack-integrity"
version = "0.1.0"
description = "Deterministic file integrity CLI"
requires-python = ">=3.9"

[project.scripts]
winstack = "winstack_integrity.cli:main"
